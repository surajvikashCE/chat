<RelativeLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    
    xmlns:android="http://schemas.android.com/apk/res/android" >

    <TextView
        android:id="@+id/custom_bar_display_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="22sp"
        android:layout_toLeftOf="@id/custom_appbar_card"
        android:text="Name"
        android:textColor="#FFFFFF"/>

    <TextView
        android:id="@+id/custom_bar_last_seen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/custom_appbar_card"
        android:layout_below="@id/custom_bar_display_name"
        android:text="Last seen"
        android:textSize="12sp"
        android:textColor="#FFFFFF"/>

    <android.support.v7.widget.CardView
        android:id="@+id/custom_appbar_card"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginRight="12dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:elevation="2dp"
        android:layout_gravity="center"
        app:cardCornerRadius="18dp"
        android:innerRadius="0dp"
        android:shape="ring"
        android:thicknessRatio="1.9">

        <com.facebook.drawee.view.SimpleDraweeView
            android:id="@+id/custom_bar_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@mipmap/londa"
            app:actualImageScaleType="centerCrop"
            app:placeholderImage="@mipmap/londa" />

    </android.support.v7.widget.CardView>
    
    
</RelativeLayout>